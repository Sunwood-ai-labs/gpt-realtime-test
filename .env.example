# Copy to .env and fill with real secrets
OPENAI_API_KEY=your-openai-api-key
MCP_SERVER_URL=https://localhost:8000
MCP_SERVER_LABEL=local-mcp-server
MCP_REQUIRE_APPROVAL=never
# MCP_AUTHORIZATION=Bearer example-token (only if your MCP endpoint needs it)
